name: factory
description: Llama AI Factory for Android
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The AI Engine
  llama_flutter_android: ^0.1.1

  # Permission manager to stop the "Instant Close" crash
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
